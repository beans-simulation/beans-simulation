import { Timer } from "@/models";

export const magnitude_mutacao = 0.1; // magnitude da mutação (o quanto vai variar)

export const probabilidade_mutacao = 0.3;

export const global_timer = new Timer();

export const universe_width = 1280;
export const universe_height = 720;
